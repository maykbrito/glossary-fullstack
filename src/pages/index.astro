---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';

import { getCollection } from 'astro:content'
const glossary = await getCollection('glossary')
---

<Layout title="Welcome to Astro.">
<div class="max-w-lg mx-auto space-y-8 mt-6">
<header>
	<h1 class="text-2xl font-bold">Glossário Full-stack</h1>
	<p class="text-gray-300/9s0">
		Descubra os termos mais comuns do front e back-end
	</p>
</header>
	<main>
		<ul class="space-y-6">
			{glossary.map(({data}) => <li>
			<strong>{data.title}</strong><br/>
			
			<p class="text-gray-300/90 hover:text-white">{data.description}</p>
			</li>)}
		</ul>
	</main>

	<footer class="text-center text-gray-400 pb-6 border-t pt-6 border-gray-100/10">feito com 💜 maykbrito</footer>
</div>
</Layout>
